# Lab 8 Docker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or implement task-by-task.

**Goal:** Full stack via `docker compose up --build` on branch `28`.

**Architecture:** Root backend Dockerfile; `client/` multi-stage nginx; compose with db+mongo; `.env` + `/health`.

**Tech Stack:** Docker, Compose, node:18-alpine, nginx:alpine, postgres:16-alpine, mongo:7

## Global Constraints

- Branch name exactly `28`
- Keep Socket.IO + dual DB working
- No emoji; match existing style
- Report KV skipped later

## File map

| File | Role |
|------|------|
| `Dockerfile` | backend production image |
| `.dockerignore` | exclude node_modules, client, .git |
| `client/Dockerfile` | multi-stage Vite → nginx |
| `client/.dockerignore` | client ignores |
| `client/nginx.conf` | SPA fallback |
| `docker-compose.yml` | all 4 services |
| `.env.example` | docker-ready vars |
| `server.js` | `GET /health` |
| entrypoint or compose command | migrate on start (optional) |
| `README.md` | compose instructions |

---

### Task 1: Branch + health endpoint
- [ ] `git checkout -b 28` from `27`
- [ ] Add `GET /health`
- [ ] Smoke curl after local start

### Task 2: Backend Dockerfile
- [ ] Root Dockerfile + .dockerignore
- [ ] npm ci / production-friendly install

### Task 3: Frontend Dockerfile
- [ ] Multi-stage + nginx.conf SPA
- [ ] ARG VITE_API_URL

### Task 4: Compose + env
- [ ] Services backend/frontend/db/mongo
- [ ] healthcheck, depends_on, volumes, env_file
- [ ] README

### Task 5: Verify
- [ ] `docker compose up --build -d`
- [ ] docker ps, localhost, /health
- [ ] Push when asked
